---
sampler:
    test_holdout:
        - 8
        - 9
    validation_holdout:
        - 6
        - 7
    random_seed: 127
    sequence_length: 1001
    center_bin_to_predict: 201
    default_threshold: 0.5
    mode: train
model_controller:
    batch_size: 64
    n_epochs: 1000
    n_steps_per_epoch: 16000
    optional_args:
        use_cuda: True
        data_parallel: False
    checkpoint:
        resume: False
...
